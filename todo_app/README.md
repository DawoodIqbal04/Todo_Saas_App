# Todo App

This is a simple in-memory CLI todo application.

## How to run

To run the application, navigate to the project root directory (`GIAIC_Hackathon_02`) and use the following command:

```bash
uv run -m todo_app.main
```
